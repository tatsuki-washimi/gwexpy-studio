# GWexpy Studio Ubuntu試用フィードバック

対象はnative Ubuntu 24.04 x86_64の`ubuntu24-x86_64`です。
分かる範囲で記入してください。
## 環境

- Ubuntu versionとdesktop環境:
- `ID` / `VERSION_ID`:
- architecture（x86_64）:
- WSLではないことの確認:
- Python / conda / pipのversion:
- ZIP名と外側checksum（成功 / 失敗）:
- 内部`SHA256SUMS`（成功 / 失敗）:
- `libEGL.so.1` / `libGL.so.1` preflight（成功 / 失敗）:
- 追加library不足の表示（なし / あり、表示内容）:

## 導入と起動

- `--only-binary=:all:`と`constraints-ubuntu24-x86_64.txt`を変更せずinstallできた（はい / いいえ）:
- conda Python 3.12環境を使用した（はい / いいえ）:
- `PYTHONNOUSERSITE=1`を設定し、`PYTHONPATH`をunsetした（はい / いいえ）:
- Launch結果（成功 / 失敗）:
- Welcomeが表示された（はい / いいえ）:

## Workflow

Welcome表示後に**Try Sample → Load → Crop → ASD → Save → Close → Open**を実行した結果を記録してください。

- Try Sample / Load / Crop / ASDの結果:
- Save → Close → Openの結果:
- Workflowの完了（はい / いいえ）:
- 完了または停止した操作段階:
- Review表示（あり / なし）と、表示時に継続できたか:
- Restore表示（あり / なし）と、表示時に継続できたか:
- About画面のBuild ID:

## 操作性と問題

- 表示倍率、native file dialog、Unicodeまたは空白を含むpathで気付いた点:
- 分かりにくかった表示、用語、操作:
- エラーメッセージと、その直前の操作:
- 時系列CSVの読込以外の操作を試した場合の表示:
- 自由記述:

エラー表示と直前の操作を案内者へ送る前に、ユーザー名、ホスト名、個人の保存場所、認証情報、機密の測定データを伏せてください。

記入後は、案内されたメールまたはchatへ返信してください。
GitHub accountは不要です。
