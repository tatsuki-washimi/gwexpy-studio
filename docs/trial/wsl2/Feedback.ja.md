# GWexpy Studio WSL2試用フィードバック

対象は`wsl2-ubuntu24`です。分かる範囲で記入し、案内されたメールまたはチャットへ
返信してください。GitHubアカウントは不要です。

## 環境と配布物

- Windows 11の表示version:
- Windows architecture（AMD64 / ARM64）:
- Windows ProductType（1のみ）:
- WSL2 Ubuntuのversion:
- WSL2側architecture（x86_64 / aarch64）:
- WSL kernel:
- WSLgの確認（成功 / 失敗）:
- Python / conda / pipのversion:
- ZIP名:
- 外側checksum（成功 / 失敗）:
- 内部`SHA256SUMS`（成功 / 失敗）:
- `libEGL.so.1` / `libGL.so.1` preflight（成功 / 失敗）:
- 対応constraintsとwheelを一つずつ確認できた（はい / いいえ）:
- `--only-binary=:all:`と`constraints-ubuntu24-${architecture}.txt`を変更せず導入した（はい / いいえ）:
- install（成功 / 失敗）:

## Workflow

- Welcomeが表示された（はい / いいえ）:
- Try Sample / Load / Crop / ASDの結果:
- Save → Close → Openの結果:
- 完了または停止した操作段階:
- Review表示（あり / なし）と、表示時に継続できたか:
- Restore表示（あり / なし）と、表示時に継続できたか:
- About画面のBuild ID:

## 操作性と問題

- clipboardで気付いた点:
- 表示倍率（DPI / scaling）で気付いた点:
- Linux側path、`/mnt/c`、日本語または空白を含むpathで気付いた点:
- 分かりにくかった表示、用語、操作:
- エラーメッセージと、その直前の操作:
- TimeSeriesのCSV読込、利用不可表示、実行制御で気付いた点:
- 自由記述:

install、launch、project reopen、recoveryで進行不能になった場合は、回避策を試さず、
送付前にユーザー名、ホスト名、個人の保存場所、認証情報、機密の測定データを伏せて
ください。その時点の画面、端末出力、停止した操作段階、直前の操作を案内者へ送付して
ください。username、hostname、端末固有path、無関係なlogも送らないでください。
