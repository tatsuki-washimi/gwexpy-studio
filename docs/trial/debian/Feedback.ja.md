# GWexpy Studio Debian 13試用フィードバック

対象はDebian 13 x86_64の`debian13-x86_64`です。
分かる範囲で記入してください。

## 環境

- OSの`ID` / `VERSION_ID` / `PRETTY_NAME`:
- CPU（x86_64）:
- Debianのデスクトップで操作した（はい / いいえ）:
- WSLの確認（成功 / 失敗）:
- Python / conda / pipのバージョン:
- ZIP名と外側チェックサム（成功 / 失敗）:
- 内部`SHA256SUMS`（成功 / 失敗）:
- `libEGL.so.1` / `libGL.so.1`の確認（成功 / 失敗）:
- 不足しているライブラリの表示（あれば）:

## 導入と起動

- `--only-binary=:all:`と`constraints-debian13-x86_64.txt`を変更せずにインストールできた（はい / いいえ）:
- Welcomeが表示された（はい / いいえ）:

## Workflow

- Try Sample / Load / Crop / ASDの結果:
- Save → Close → Openの結果:
- Workflow完了（はい / いいえ）:
- 完了または停止した操作段階:
- About画面のBuild ID:
- 時系列CSVの読み込み結果:
- 利用できなかった操作と画面の表示:

## 操作性と問題

- ファイル選択画面、文字の大きさ、表示倍率、保存場所で気付いた点:
- 分かりにくかった表示、用語、操作:
- エラーメッセージと、その直前の操作:
- 今後使いたい解析機能:
- 自由記述:

インストール、起動、プロジェクトの再オープン、復旧で進行できなくなった場合は、表示された内容と直前の操作を案内元へ知らせてください。

案内されたメールまたはチャットへ返信してください。
GitHubアカウントは不要です。
送る前に、ユーザー名、ホスト名、個人の保存場所、認証情報、機密の測定データを伏せ、エラー表示と直前の操作を案内者へ伝えてください。
