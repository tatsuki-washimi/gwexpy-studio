[pytest]
addopts = --import-mode=importlib
testpaths = gui_tests
markers =
    gui: GUI-specific test
    contract(id): one executable requirement contract item
    explicit_capability_handshake: test owns the worker startup handshake
